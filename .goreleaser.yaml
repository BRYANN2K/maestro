version: 2

project_name: maestro

before:
  hooks:
    - bun scripts/build-runtime-all.ts

builds:
  - id: maestro
    main: ./cmd/maestro
    binary: maestro
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X github.com/bryann2k/maestro/internal/agentcore.Version={{ .Version }}

archives:
  - id: maestro
    ids:
      - maestro
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - src: "bin/release/{{ .Os }}_{{ .Arch }}/*"
        strip_parent: true
      - src: "runtime/LICENSE.oh-my-pi"
      - src: "runtime/upstream.json"
      - LICENSE
      - THIRD_PARTY_NOTICES.md
      - LICENSES/**
      - RUNTIME_LICENSES/**
      - README.md
      - CHANGELOG.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"

release:
  github:
    owner: BRYANN2K
    name: maestro
  prerelease: auto

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"
